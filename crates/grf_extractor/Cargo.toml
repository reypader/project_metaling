[package]
name = "grf_extractor"
version = "0.1.0"
edition = "2024"

[dependencies]
ro_files = { path = "../ro_files" }
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
anyhow = "1"
encoding_rs = "0.8"
